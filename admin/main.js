$(()=>{
  console.log("testing admin/main.js");

  // Lets user see all posts or ONLY those waiting for approval
  // if ($("[data-approval]").attr('approval') == "true") {
  //   $("[data-approval]").css('display','none');
  // };

});
